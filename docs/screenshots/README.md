Screenshots referenced by the root README.

Expected files:

- `dashboard.png`    — the dashboard with all four sections, signed in as the demo account
- `onboarding.png`   — the onboarding quiz with the searchable asset picker
- `preferences.png`  — the editable preferences screen
- `degraded.png`     — a section showing its fallback state with the notice visible

Capture them from the live app rather than localhost, so they show real data.
